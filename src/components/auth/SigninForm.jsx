import logo from "../../../public/images/signin.png";

const SigninForm = () => {
  return (
    <div backgroundImage={`url(${logo})`} className="min-h-screen">
      <h2>Sign In</h2>
    </div>
  );
};

export default SigninForm;
